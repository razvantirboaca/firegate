# Firegate Backend (if using a separate Express/Node server)

# Server config
PORT=3000

# OpenAI
OPENAI_API_KEY=your-openai-api-key

# (Optional) Firebase Admin SDK or other backend keys here
# FIREBASE_ADMIN_KEY=...